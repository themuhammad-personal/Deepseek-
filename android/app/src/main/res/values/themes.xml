<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.SuperDeepSeek" parent="Theme.Material3.DayNight" />

    <!-- Status/navigation bar colors and light-bar appearance are set programmatically in
         MainActivity so they react to the page's live dark/light theme. The window
         background is the deep midnight launch color: when the system splash hands
         off to the window, the next frame is already our brand — never white. -->
    <style name="Theme.SuperDeepSeek.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/bds_window_bg</item>
        <item name="android:colorBackground">@color/bds_window_bg</item>
    </style>

    <style name="Theme.SuperDeepSeek.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/bds_splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.SuperDeepSeek.NoActionBar</item>
    </style>
</resources>
