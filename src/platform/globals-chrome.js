/**
 * Platform globals entry for the chrome/firefox builds.
 *
 * The Vite alias `bds-platform-globals` resolves to this file by default, so
 * the import in src/content/index.js is a no-op on extension targets.
 *
 * The android build swaps the alias to globals-android.js (see build.js).
 */
