{
  "name": "super-deepseek-v2",
  "version": "2.0.0",
  "description": "Super DeepSeek V2 - Completely rebuilt from scratch, exact UI copy of demo, no hangs",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "build:android": "vite build && node scripts/copy-v2-assets.js",
    "preview": "vite preview",
    "android:assemble:debug": "npm run build:android && cd android && ./gradlew assembleDebug",
    "android:assemble:release": "npm run build:android && cd android && ./gradlew assembleRelease"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "svelte": "^5.28.2",
    "vite": "^6.3.1"
  },
  "dependencies": {
    "marked": "^18.0.3"
  }
}
